import React, {Component} from 'react';import {connect} from 'react-redux';import FetchData from "../FetchData";import {fetchForecasts} from "../../store/FetchData/actions";class FetchDataContainer extends Component {    render() {        return (<FetchData loading={this.props.loading} forecasts={this.props.forecasts}                           fetchForecasts={this.props.fetchForecasts}/>)    }}const putStateToProps = (state, ownProps) => {    return {        forecasts: state.fetchdata.forecasts,        loading: state.fetchdata.loading    };}const linkActionsToProps = {    fetchForecasts}export default connect(putStateToProps, linkActionsToProps)(FetchDataContainer);